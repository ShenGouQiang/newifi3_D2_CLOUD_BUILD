name: OpenWrt Cloud Build
on: 
  push
jobs:
  My First jobs:
    name: my_first_job
    runs-on: ubuntu-18.04
    steps:
      - name: checkout
        run: git clone https://github.com/coolsnowwolf/lede.git
      - name: list show
        run: echo `ls ./`
      - name: show path
        run: echo `pwd`